# Dir2Clip
A lightweight CLI utility that recursively parses a directory, flattens file contents into a formatted string, and copies the result directly to your clipboard for seamless use with LLMs like ChatGPT, Claude, or local models.
